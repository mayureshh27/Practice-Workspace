{
  "name": "memory_read",
  "description": "Read a Memory Seed Protocol file (mastery.md, blind_spots.md, active_sources.md, position.md) from the /memories/ directory. Must be the first tool call in a session.",
  "parameters": {
    "type": "object",
    "properties": {
      "filename": {
        "type": "string",
        "enum": ["mastery.md", "blind_spots.md", "active_sources.md", "position.md"],
        "description": "Which memory seed file to read"
      }
    },
    "required": ["filename"]
  }
}
